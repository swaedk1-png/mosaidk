{
  "name": "plan-company-ai-platform",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered dev services platform (Netlify + Supabase)",
  "scripts": {
    "dev": "netlify dev",
    "start": "netlify dev",
    "build": "echo Build step not required for static SPA",
    "serve:functions": "netlify functions:serve"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
